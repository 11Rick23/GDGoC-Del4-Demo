export const SYSTEM_PROMPT = `
あなたは優しいAIアシスタントです。
ユーザーの質問に対して、短く優しく返信してください。
口調はタメ語でお願いします。
必要なら具体例を出し、不要に長くなりすぎないでください。
`.trim();

//挨拶
export const FIRST_GREETING = `初めまして！ハロメイトへようこそ！`.trim();

export const SECOND_GREETING = `おかえりなさい！理想の友達像についてもっと教えてね`.trim();

//質問文
export const ASK_CHARACTOR_MYSELF_TEXT = `どんな性格の人と友達になりたいか教えてね`.trim();

export const ASK_HOBBY_TEXT = `次に、どんな趣味の人と友達になりたいか教えてね`.trim();

export const ASK_CHARACTOR_BYFRIEND_TEXT = `じゃあ、どんな人と一緒にいると楽しかったり心地いいかな`.trim();

export const ASK_AVOID_TYPE_TEXT = `最後に、苦手な人や友達になりたくないタイプの人がいればここで詳しく教えてね`.trim();

export const FINAL_COMPLETE_TEXT = `これで入力は完了だよ。あなたにぴったりな友達を探してるからちょっと待ってね`.trim();

export const EDIT_PROFILE_TEXT = `理想の友達像について、今後もっと伝えたいことがあればここで教えてね`.trim();

export const EDIT_COMPLETE_TEXT = `更新したい内容を反映したよ。ほかにもあれば続けて教えてね。`.trim();

//要素抽出用プロンプト
export const EXTRACT_HOBBY_PROMPT = `
次のユーザー入力から友達になりたい相手の趣味として自然に判断できる場合だけその行為・名称を抽出してください。

ルール:
- 趣味だと判断できる場合は \`HOBBY: <行為・名称>\` の形式だけで返す
- 複数ある場合は \`HOBBY: <行為・名称>/<行為・名称>\` のように/で区切る
- 趣味だと判断できない場合は \`RETRY\` だけ返す
- 説明や補足は一切書かない

ユーザー入力: {user_input}
`.trim();


export const EXTRACT_CHARACTOR_MYSELF_PROMPT = `
次のユーザー入力から性格や性質として自然に判断できる場合だけその性格や性質を抽出してください。

ルール:
- 性格や性質だと判断できる場合は \`CHARACTOR_MYSELF: <性格や性質>\` の形式だけで返す
- 複数ある場合は \`CHARACTOR_MYSELF: <性格や性質>/<性格や性質>\` のように/で区切る
- 性格や性質だと判断できない場合は \`RETRY\` だけ返す
- 説明や補足は一切書かない

ユーザー入力: {user_input}
`.trim();


export const EXTRACT_CHARACTOR_BYFRIEND_PROMPT = `
次のユーザー入力から、一緒にいると楽しかったり心地よいと感じる相手の性格や性質として自然に判断できる場合だけ抽出してください。

ルール:
- 性格や性質だと判断できる場合は \`CHARACTOR_BYFRIEND: <性格や性質>\` の形式だけで返す
- 複数ある場合は \`CHARACTOR_BYFRIEND: <性格や性質>/<性格や性質>\` のように/で区切る
- 性格や性質だと判断できない場合は \`RETRY\` だけ返す
- 説明や補足は一切書かない

ユーザー入力: {user_input}
`.trim();

export const EXTRACT_AVOID_TYPE_PROMPT = `
次のユーザー入力から、苦手な人や友達になりたくないタイプの人の性格・性質・行動傾向として自然に判断できる場合だけ抽出してください。

ルール:
- 性格・性質・行動傾向だと判断できる場合は \`AVOID_TYPE: <性格・性質・行動傾向>\` の形式だけで返す
- 複数ある場合は \`AVOID_TYPE: <性格・性質・行動傾向>/<性格・性質・行動傾向>\` のように/で区切る
- 判断できない場合は \`RETRY\` だけ返す
- 説明や補足は一切書かない

ユーザー入力: {user_input}
`.trim();

export const HOBBIES_COMMENT_PROMPT = `
ユーザーが友達になりたい相手の趣味は {hobbies} です。
この内容に対して優しく一文でコメントしてください。できるだけ抽象的なコメントにしてください。
`.trim();


export const CHARACTOR_MYSELF_COMMENT_PROMPT = `
ユーザーが友達になりたい相手の性格は {charactors} です。
この内容に対して優しく一文でコメントしてください。できるだけ抽象的なコメントにしてください。
`.trim();


export const CHARACTOR_BYFRIEND_COMMENT_PROMPT = `
ユーザーが一緒にいると心地いいと感じる相手の性格や性質は {charactors} です。
この内容に対して優しく一文でコメントしてください。できるだけ抽象的なコメントにしてください。
`.trim();


//プロフィール分作成用プロンプト
export const CREATE_PROFILE_TEXT = `
次のユーザー情報から、ベクトル検索で使うための検索用人物特徴文を日本語で作成してください。

  ルール:
  - 出力は検索用の特徴文にすること
  - 出力は、ユーザーが友達になりたい相手の特徴を説明する文章にすること
  - ユーザー本人の説明にはしないこと
  - 性格の特徴、趣味について含めること
  - 「理想の性格」と「一緒にいて心地いい性格・性質」が両方ある場合は、共通点と違いを踏まえて総合的にまとめること
  - 入力にある語をなるべく残すこと
  - 入力にない情報は足さないこと
  - 「理想です」「嬉しいです」などの感想表現は避けること
  - 誇張した表現や文学的な表現は避けること
  - ベクトル検索に使いやすいよう、特徴が明確に分かる文章にすること
  - 1段落、3〜5文で出力すること

  ユーザー情報: {profile_inputs}
`.trim();

export const CREATE_AVOID_PROFILE_TEXT = `
次のユーザー情報から、ベクトル検索で使うための「苦手な人・友達になりたくない相手」の特徴文を日本語で作成してください。

ルール:
- 出力は検索用の特徴文にすること
- 出力は、ユーザーが苦手だと感じる相手の特徴を説明する文章にすること
- 性格・性質・行動傾向が分かる文章にすること
- 入力にある語をなるべく残すこと
- 入力にない情報は足さないこと
- 「苦手です」「避けたいです」などの感想表現は避けること
- 誇張した表現や文学的な表現は避けること
- ベクトル検索に使いやすいよう、特徴が明確に分かる文章にすること
- 1段落、2〜4文で出力すること

ユーザー情報: {profile_inputs}
`.trim();

export const UPDATE_PROFILE_PROMPT = `
次の情報をもとに、ユーザーが友達になりたい相手のプロフィール文と、友達になりたくない相手のプロフィール文を更新してください。

ルール:
- まず、ユーザーの追加入力が「友達になりたい相手」についての修正か、「友達になりたくない相手」についての修正か、または両方かを判断すること
- さらに、ユーザーの追加入力が「プロフィール文の更新」ではなく、「今すぐその条件で相手を探したい」「もっとこんな人でマッチしたい」など、マッチング実行を求める意図かどうかも判断すること
- 判断した対象のプロフィール文だけを更新し、対象外のプロフィール文は意味を変えずに維持すること
- 入力にない情報を足さないこと
- 既存のプロフィール文の文体と用途を保つこと
- 出力は必ず次の3行だけにすること
- 1行目は \`DESIRED_PROFILE: <更新後の友達になりたい相手のプロフィール文>\`
- 2行目は \`UNDESIRED_PROFILE: <更新後の友達になりたくない相手のプロフィール文>\`
- 3行目は \`MATCH: UPDATE\` または \`MATCH: MATCH\` のどちらかにすること
- ユーザーの追加入力がプロフィール更新の指示なら \`MATCH: UPDATE\`
- ユーザーの追加入力が「探して」「もっとこんな人がいい」「この条件でマッチしたい」など、マッチングを求める指示なら \`MATCH: MATCH\`
- 説明や補足は一切書かないこと

現在の友達になりたい相手のプロフィール文: {desired_profile}
現在の友達になりたくない相手のプロフィール文: {undesired_profile}
ユーザーの追加入力: {edit_input}
`.trim();

export const MATCH_REASON_PROMPT = `
次の2つのプロフィール文を比較して、相性が良さそうな点を日本語で1文だけ説明してください。

ルール:
- 必ず1文にすること
- 居住地、学校名などは記載しないこと
- 「ここがマッチしました」と分かる自然な説明にすること
- 入力にない内容は足さないこと
- 短く、具体的にすること.

求める相手のプロフィール文: {desired_profile}
相手のプロフィール文: {candidate_profile}
`.trim();
