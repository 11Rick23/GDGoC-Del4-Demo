export const SYSTEM_PROMPT = `
あなたは優しいAIアシスタントです。
ユーザーの質問に対して、短く優しく返信してください。
口調はタメ語でお願いします。
必要なら具体例を出し、不要に長くなりすぎないでください。
`.trim();

//挨拶
export const FIRST_GREETING = `初めまして！ハロメイトへようこそ！`.trim();

export const SECOND_GREETING = `おかえりなさい！今日もあなたのことについて教えて欲しいな`.trim();

//質問文
export const ASK_LIVE_TEXT = `まず、住んでる地域を教えてね！答えたくなければスキップと答えてね！`.trim();

export const ASK_JOB_TEXT = `次に、学校や企業の名前を教えて！答えたくなければスキップと答えてね`.trim();

export const ASK_LIKES_TEXT = `じゃあ、もっとあなた自身のことを聞いていくね。好きなことやものは何かな？`.trim();

export const ASK_HOBBY_TEXT = `次に、休日や暇なときは何をしていることが多い？`.trim();

export const ASK_CHARACTOR_MYSELF_TEXT = `自分自身を一言で表すと、どんな感じ？`.trim();

export const ASK_CHARACTOR_BYFRIEND_TEXT = `友達や周りの人にはどんな性格だと言われることが多い？3~4つくらい教えて欲しいな`.trim();

export const ASK_BELIEF_RELATIONSHIP_TEXT = `最後に人間関係で大切にしていることを教えてね`.trim();

export const FINAL_COMPLETE_TEXT = `これで入力は完了だよ。`.trim();

export const EDIT_PROFILE_TEXT = `あなた自身についてもっと伝えたいことがあれば教えてね`.trim();

export const EDIT_COMPLETE_TEXT = `更新したい内容を反映したよ。ほかにもあれば続けて教えてね。`.trim();

//要素抽出用プロンプト
export const EXTRACT_LIVE_PROMPT = `
次のユーザー入力から住んでる地域として自然に判断できる場合だけ地域を抽出してください。

ルール:
- 地域だと判断できる場合は \`LIVE: <地域>\` の形式だけで返す
- 複数ある場合は現在と判断できる場所か、最も詳細な場所の一つを選ぶ
- 地域だと判断できない場合は \`SKIP\` だけ返す
- \`スキップ\`が含まれる場合は \`SKIP\` だけ返す
- 説明や補足は一切書かない

ユーザー入力: {user_input}
`.trim();


export const EXTRACT_JOB_PROMPT = `
次のユーザー入力から現在の仕事として自然に判断できる場合だけ仕事を抽出してください。

ルール:
- 地域だと判断できる場合は \`JOB: <仕事>\` の形式だけで返す
- 複数ある場合は \`JOB: <仕事>/<仕事>\` のように/で区切る
- 仕事だと判断できない場合は \`SKIP\` だけ返す
- \`スキップ\`が含まれる場合は \`SKIP\` だけ返す
- 説明や補足は一切書かない

ユーザー入力: {user_input}
`.trim();


export const EXTRACT_LIKE_PROMPT = `
次のユーザー入力から好きなことやものとして自然に判断できる場合だけ事物を抽出してください。

ルール:
- 好きなことやものだと判断できる場合は \`LIKE: <事物>\` の形式だけで返す
- 複数ある場合は \`LIKE: <事物>/<事物>\` のように/で区切る
- 好きなものやことだと判断できない場合は \`RETRY\` だけ返す
- 説明や補足は一切書かない

ユーザー入力: {user_input}
`.trim();


export const EXTRACT_HOBBY_PROMPT = `
次のユーザー入力から休日や暇な時にすることとして自然に判断できる場合だけその行為・名称を抽出してください。

ルール:
- 休日や暇な時にすることだと判断できる場合は \`HOBBY: <行為・名称>\` の形式だけで返す
- 複数ある場合は \`HOBBY: <行為・名称>/<行為・名称>\` のように/で区切る
- 休日や暇な時にすることだと判断できない場合は \`RETRY\` だけ返す
- 説明や補足は一切書かない

ユーザー入力: {user_input}
`.trim();


export const EXTRACT_CHARACTOR_MYSELF_PROMPT = `
次のユーザー入力から性格や性質として自然に判断できる場合だけその性格や性質を抽出してください。

ルール:
- 性格や性質だと判断できる場合は \`CHARACTOR_MYSELF: <性格や性質>\` の形式だけで返す
- 複数ある場合は \`CHARACTOR_MYSELF: <性格や性質>/<性格や性質>\` のように/で区切る
- 性格や性質だと判断できない場合は \`RETRY\` だけ返す
- 説明や補足は一切書かない

ユーザー入力: {user_input}
`.trim();


export const EXTRACT_CHARACTOR_BYFRIEND_PROMPT = `
次のユーザー入力から性格や性質として自然に判断できる場合だけその性格や性質を抽出してください。

ルール:
- 性格や性質だと判断できる場合は \`CHARACTOR_BYFRIEND: <性格や性質>\` の形式だけで返す
- 複数ある場合は \`CHARACTOR_BYFRIEND: <性格や性質>/<性格や性質>\` のように/で区切る
- 性格や性質だと判断できない場合は \`RETRY\` だけ返す
- 説明や補足は一切書かない

ユーザー入力: {user_input}
`.trim();


export const EXTRACT_BILIEF_RELATION_PROMPT = `
次のユーザー入力から人間関係で大切にしていることとして自然に判断できる場合だけその事柄を抽出してください。

ルール:
- 人間関係で大切にしていることだと判断できる場合は \`BELIEF: <事柄>\` の形式だけで返す
- 複数ある場合は \`BILIEF: <事柄>/<事柄>\` のように/で区切る
- 人間関係で大切にしていることだと判断できない場合は \`RETRY\` だけ返す
- 説明や補足は一切書かない

ユーザー入力: {user_input}
`.trim();


//コメント用プロンプト
export const LIKES_COMMENT_PROMPT = `
ユーザーの趣味は {likes} です。
この内容に対して優しく一文でコメントしてください。できるだけ抽象的なコメントにしてください。
`.trim();


export const HOBBIES_COMMENT_PROMPT = `
ユーザーの趣味は {hobbies} です。
この内容に対して優しく一文でコメントしてください。できるだけ抽象的なコメントにしてください。
`.trim();


export const CHARACTOR_MYSELF_COMMENT_PROMPT = `
ユーザーの趣味は {charactors} です。
この内容に対して優しく一文でコメントしてください。できるだけ抽象的なコメントにしてください。
`.trim();


export const CHARACTOR_BYFRIEND_COMMENT_PROMPT = `
ユーザーの趣味は {charactors} です。
この内容に対して優しく一文でコメントしてください。できるだけ抽象的なコメントにしてください。
`.trim();


//プロフィール分作成用プロンプト
export const CREATE_PROFILE_TEXT = `
次のユーザー情報から、ベクトル検索で使うための人物特徴文を日本語で作成してください。

  ルール:
  - 出力は検索用の特徴文にすること
  - 出力は、ユーザー本人の特徴を説明する文章にすること
  - 自己紹介文ではなく、第三者が読んで人物像を把握できる説明文にすること
  - 性格、趣味や関心、行動傾向を含めること
  - 「好きなこと・もの」と「休日や暇なときにしていること」は重複している場合は重複を取り除いて、総合的にまとめること
  - 「自分の思う自分の性格」と「他者から見た性格」が両方ある場合は、共通点と違いを踏まえて総合的にまとめること
  - 共通している特徴は、その人をよく表す特徴として優先的に扱うこと
  - 違いがある場合は矛盾として捨てず、その人の多面的な特徴として自然に統合すること
  - 入力にある語をなるべく残すこと
  - 入力にない情報は足さないこと
  - 「理想です」「嬉しいです」などの感想表現は避けること
  - 誇張した表現や文学的な表現は避けること
  - ベクトル検索に使いやすいよう、特徴が明確に分かる文章にすること
  - 1段落、3〜5文で出力すること

ユーザ情報: {profile_inputs}
`.trim();

export const UPDATE_PROFILE_PROMPT = `
次の既存プロフィール文と、ユーザーが追加で伝えた内容をもとに、人物特徴文を更新してください。

ルール:
- 既存プロフィール文をベースに更新すること
- ユーザーの追加内容を反映すること
- 入力にない情報は新たに足さないこと
- 既存プロフィール文にある特徴は、追加内容と矛盾しない限り残すこと
- ベクトル検索で使うための人物特徴文として、特徴が明確に分かる文章にすること
- 自己紹介文ではなく、第三者が読んで人物像を把握できる説明文にすること
- 1段落、3〜5文で出力すること

既存プロフィール文: {current_profile}
追加内容: {edit_input}
`.trim();
